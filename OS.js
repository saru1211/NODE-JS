const os = require("os")


console.log(os.arch()) //method to find the architecture we working on

console.log(os.platform()) //method to find the platform we working on

console.log(os.networkInterfaces()) //method that tell you network details u working on

console.log(os.cpus()) // tells you the details of ur CPU

console.log(os.totalmem()) //tells you the total memory that ur system have

console.log(os.freemem()) //tells you the free memory that ur system have